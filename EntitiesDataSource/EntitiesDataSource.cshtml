@using Sitecore.Mvc
@using Sitecore.Mvc.Presentation
@using Sitecore.Web.UI.Controls.Common.UserControls
@model RenderingModel
@{
    var userControl = this.Html.Sitecore().Controls().GetUserControl(this.Model.Rendering);

    userControl.Requires.Script("mikerobbins", "entitiesdatasource.js");

    userControl.Attributes["data-sc-serviceURL"] = userControl.GetString("ServiceUrl", "serviceUrl");
    userControl.Attributes["data-sc-isBusy"] = userControl.GetString("IsBusy", "isBusy");

    var controlId = userControl.ControlId;
    var htmlAttributes = userControl.HtmlAttributes;
}
<script @htmlAttributes type="text/x-sitecore-entitiesdatasource">
</script>